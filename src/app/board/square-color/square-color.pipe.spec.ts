import { SquareColorPipe } from './square-color.pipe';

describe('SquareColorPipe', () => {
  it('create an instance', () => {
    const pipe = new SquareColorPipe();
    expect(pipe).toBeTruthy();
  });
});
